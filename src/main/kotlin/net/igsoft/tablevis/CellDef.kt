package net.igsoft.tablevis

/*
class CellDef<S : Style, T : StyleSet<S>>(private val rowDef: RowDef<S, T>) {
    var width: Int? = null
    var height: Int? = null

    var minimalTextWidth = rowDef.minimalTextWidth

    var leftMargin = rowDef.leftMargin
    var topMargin = rowDef.topMargin
    var rightMargin = rowDef.rightMargin
    var bottomMargin = rowDef.bottomMargin

    var text = ""

    fun id(vararg ids: Any) {
        this.ids = ids.toList()
    }

    fun alignCenter() = apply {
        this.horizontalAlignment = HorizontalAlignment.Center
    }

    fun alignLeft() = apply {
        this.horizontalAlignment = HorizontalAlignment.Left
    }

    fun alignRight() = apply {
        this.horizontalAlignment = HorizontalAlignment.Right
    }

    fun justify() = apply {
        this.horizontalAlignment = HorizontalAlignment.Justified
    }

    fun alignTop() = apply {
        this.verticalAlignment = VerticalAlignment.Top
    }

    fun alignMiddle() = apply {
        this.verticalAlignment = VerticalAlignment.Middle
    }

    fun alignBottom() = apply {
        this.verticalAlignment = VerticalAlignment.Bottom
    }

    //------------------------------------------------------------------------------------------------------------------
    //Implementation code

    private lateinit var ids: List<Any>
    private var horizontalAlignment = rowDef.horizontalAlignment
    private var verticalAlignment = rowDef.verticalAlignment
}
*/
